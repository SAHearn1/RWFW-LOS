TITLE: [#PR-206] Restore SQS+Dynamo as primary worker backend in production
LABELS: type:bug, risk:high, area:cloud, area:runtime, agent:solo
SCOPE:
- Fix production worker backend so AWS path is primary.
- Keep in-memory fallback as controlled contingency only.
OUT OF SCOPE:
- New job types.
DEPENDENCIES:
- #PR-205 preferred before merge.
IMPLEMENTATION NOTES:
- Ensure backend decision is observable in response metadata.
ACCEPTANCE CRITERIA:
- Cloud smoke reports `workerQueueBackend=sqs` and `workerStoreBackend=dynamodb` in production.
TEST PLAN:
- Commands: `npm run lint && npm run build && npm run verify:cloud-aws-smoke`
- Verify route: POST `/api/orchestration/worker-run`
FILES/AREAS TO TOUCH:
- `app/api/orchestration/worker-run/*`, `lib/orchestration/*`
ROLLBACK PLAN:
- Controlled fallback to in-memory with explicit warning metadata.
AGENT GUARDRAILS:
- Do not change federation route contract.
