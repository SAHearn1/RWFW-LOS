TITLE: [#PR-201] Serverless-safe ledger persistence on Vercel
LABELS: type:bug, risk:high, area:ledger, area:data, agent:solo
SCOPE:
- Remove production dependency on local SQLite filesystem writes.
- Ensure ledger writes/reads use serverless-safe persistence path.
OUT OF SCOPE:
- New ledger schema redesign.
DEPENDENCIES:
- None.
IMPLEMENTATION NOTES:
- Keep adapter contract stable; prefer env-driven backend selection.
ACCEPTANCE CRITERIA:
- No EACCES filesystem write attempts in production ledger flow.
- Ledger API still returns existing response shape.
TEST PLAN:
- Commands: `npm run lint && npm run build`
- Verify routes: POST/GET `/api/ledger/records`
FILES/AREAS TO TOUCH:
- `lib/ledger/*`, `app/api/ledger/*`
ROLLBACK PLAN:
- Re-enable previous adapter path by feature flag/env toggle.
AGENT GUARDRAILS:
- Do not modify auth logic or role model.
- Do not edit `app/app/layout.tsx` or `app/layout.tsx`.
