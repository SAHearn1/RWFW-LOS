TITLE: [#PR-203] Clerk webhook event application for role/org propagation
LABELS: type:bug, risk:high, area:auth, area:security, agent:solo
SCOPE:
- Process verified Clerk webhook payloads for `user.created` and `user.updated`.
- Persist role/org changes into app-consumed state.
OUT OF SCOPE:
- Analytics or downstream CRM sync.
DEPENDENCIES:
- None.
IMPLEMENTATION NOTES:
- Keep HMAC verification intact; add deterministic event dispatch by type.
ACCEPTANCE CRITERIA:
- Role/org updates in Clerk affect app access behavior after webhook receipt.
- Unknown event types handled safely with explicit no-op response.
TEST PLAN:
- Commands: `npm run lint && npm run build && npm run verify:webhook-contract`
- Verify route: POST `/api/webhooks/clerk`
FILES/AREAS TO TOUCH:
- `app/api/webhooks/clerk/*`, `lib/auth/*`
ROLLBACK PLAN:
- Disable event application behind explicit env gate while keeping verification.
AGENT GUARDRAILS:
- Do not modify route protection middleware semantics outside webhook sync.
