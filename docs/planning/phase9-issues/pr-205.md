TITLE: [#PR-205] Federation POST must dispatch, not acknowledge-only
LABELS: type:bug, risk:high, area:federation, area:cloud, agent:solo
SCOPE:
- Implement deterministic dispatch path for accepted federation tasks.
- Return explicit dispatch metadata and failure semantics.
OUT OF SCOPE:
- Multi-region federation scaling.
DEPENDENCIES:
- None.
IMPLEMENTATION NOTES:
- Preserve GET discovery contract.
ACCEPTANCE CRITERIA:
- POST `/api/federation` routes task through real dispatch logic.
- Response includes execution metadata (assignee/capability/result status).
TEST PLAN:
- Commands: `npm run lint && npm run build && npm run verify:cloud-aws-smoke`
- Verify routes: GET+POST `/api/federation`
FILES/AREAS TO TOUCH:
- `app/api/federation/*`, `lib/federation/*`
ROLLBACK PLAN:
- Fallback to explicit 503 when dispatch backend unavailable.
AGENT GUARDRAILS:
- Do not edit orchestration queue adapters in this ticket.
