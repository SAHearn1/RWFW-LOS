TITLE: [#PR-207] Durable AWS credential strategy for Vercel runtime
LABELS: type:chore, risk:med, area:cloud, area:release, area:docs, agent:solo
SCOPE:
- Document and implement durable credential strategy (no expiring manual session-token dependency).
OUT OF SCOPE:
- New cloud provider migration.
DEPENDENCIES:
- #PR-206
IMPLEMENTATION NOTES:
- Include rotation SOP and incident fallback procedure.
ACCEPTANCE CRITERIA:
- Operations runbook has exact setup + rotation/recovery flow.
- Env contract and required keys documented in `.env.example`/docs.
TEST PLAN:
- Commands: `npm run lint && npm run build`
- Verify: docs review + successful post-rotation smoke run.
FILES/AREAS TO TOUCH:
- `docs/operations-runbook.md`, `infra/aws/*`, `.env.example`
ROLLBACK PLAN:
- Revert to prior env model with explicit expiration monitoring note.
AGENT GUARDRAILS:
- No application route changes in this ticket.
