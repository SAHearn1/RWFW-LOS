TITLE: [#PR-204] Enforce learner-level authorization in ledger API
LABELS: type:bug, risk:high, area:auth, area:ledger, area:security, agent:solo
SCOPE:
- Enforce learnerId authorization on ledger reads/writes.
- Preserve explicit teacher/admin access policy.
OUT OF SCOPE:
- New role introduction.
DEPENDENCIES:
- #PR-203 (if shared auth helpers are introduced).
IMPLEMENTATION NOTES:
- Centralize authorization check helper for ledger endpoints.
ACCEPTANCE CRITERIA:
- Authenticated user cannot read another learner's records.
- Teacher/admin policy matches route contract.
TEST PLAN:
- Commands: `npm run lint && npm run build`
- Verify routes: GET/POST `/api/ledger/records` for allowed/denied cases.
FILES/AREAS TO TOUCH:
- `app/api/ledger/records/*`, `lib/auth/*`
ROLLBACK PLAN:
- Revert to previous authorization helper via single commit rollback.
AGENT GUARDRAILS:
- Do not alter Clerk integration.
