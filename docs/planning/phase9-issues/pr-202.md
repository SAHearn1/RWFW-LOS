TITLE: [#PR-202] Durable audit transport for serverless runtime
LABELS: type:bug, risk:high, area:observability, area:security, agent:solo
SCOPE:
- Remove file append dependency from production audit path.
- Emit structured audit records to durable-compatible sink path.
OUT OF SCOPE:
- Full SIEM integration.
DEPENDENCIES:
- None.
IMPLEMENTATION NOTES:
- Keep audit contract shape unchanged.
ACCEPTANCE CRITERIA:
- Audit events available in production runtime path.
- No synchronous filesystem dependency in serverless code path.
TEST PLAN:
- Commands: `npm run lint && npm run build`
- Verify route that emits audit events (existing security-relevant route).
FILES/AREAS TO TOUCH:
- `lib/observability/*`, `docs/runbooks/*`
ROLLBACK PLAN:
- Fallback to stdout-only emission while preserving event schema.
AGENT GUARDRAILS:
- Do not alter business authorization rules.
- No unrelated refactors.
