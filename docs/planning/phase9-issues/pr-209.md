TITLE: [#PR-209] Teacher surfaces: Command Center, Cohorts, Reviews
LABELS: type:feature, risk:med, area:nav, agent:swarm
SCOPE:
- Replace placeholders with functional teacher-focused UI shells.
OUT OF SCOPE:
- Advanced analytics.
DEPENDENCIES:
- P1 closure required.
IMPLEMENTATION NOTES:
- Keep existing auth and route contracts unchanged.
ACCEPTANCE CRITERIA:
- Teacher role sees functional screens on each route without placeholder copy.
TEST PLAN:
- Commands: `npm run lint && npm run build`
- Verify routes: `/app/command-center`, `/app/cohorts`, `/app/reviews`
FILES/AREAS TO TOUCH:
- `app/app/command-center/*`, `app/app/cohorts/*`, `app/app/reviews/*`
ROLLBACK PLAN:
- Revert each route to previous placeholder component independently.
AGENT GUARDRAILS:
- No changes to auth provider or role semantics.
